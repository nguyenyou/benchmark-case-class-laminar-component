{
  "name": "benchmark-caseclass",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "build:scala": "scala-cli --cli-version 1.9.1 --power package . -f -o scalajs-modules.js"
  },
  "devDependencies": {
    "vite": "7.1.11"
  }
}
